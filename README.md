# 🏛 Государственный каталог музейных экспонатов "ArtSee"

Информационная система для учета, хранения и предоставления доступа к музейным ценностям. Проект разработан в рамках практической работы по изучению архитектурных шаблонов и принципов проектирования ПО.

## 📋 Описание проекта

Система представляет собой веб-приложение (Full-stack), реализующее каталог произведений искусства. Пользователи могут просматривать экспонаты различных музеев (Эрмитаж, Третьяковская галерея и др.), искать их по названию, фильтровать по типу искусства и сортировать.

**Ключевая особенность:** Проект построен на строгой архитектуре **MVC** с внедрением классических паттернов проектирования (GoF).

## 🚀 Функциональность

- **Каталог:** Просмотр списка экспонатов с пагинацией (в полной версии) или полным списком.
- **Поиск:** "Умный" поиск по названию картины или музея.
- **Фильтрация:** Серверная фильтрация по типам (Живопись, Скульптура) и музеям.
- **Сортировка:** Динамическая сортировка (по дате, по алфавиту).
- **Детальный просмотр:** Страница конкретного экспоната с описанием и характеристиками.

## 🛠 Технологический стек

- **Backend:** Node.js, Express.js
- **Frontend:** Vue.js (CDN), HTML5, CSS3
- **Архитектура:** MVC (Model-View-Controller)
- **Хранение данных:** In-Memory Database (Эмуляция БД для демонстрационных целей и простоты развертывания без установки MongoDB).

## 🏗 Архитектура и Паттерны

В проекте реализованы следующие архитектурные решения:

### 1. Архитектура MVC
- **Model (`/models`):** Описывает структуру данных экспоната.
- **View (`/public`, `/views`):** Клиентский интерфейс на Vue.js и админ-панель на EJS.
- **Controller (`/controllers`):** Обрабатывает входящие HTTP-запросы, управляет фильтрацией и логикой приложения.

### 2. Паттерны проектирования (Design Patterns)
Код, реализующий паттерны, находится в папке `/patterns`.

*   **Singleton (Одиночка):** Реализован в `config/db.js`. Гарантирует существование единственного экземпляра подключения к источнику данных.
*   **Factory Method (Фабричный метод):** Реализован в `patterns/ExhibitFactory.js`. Унифицирует создание объектов экспонатов, скрывая логику инициализации.
*   **Observer (Наблюдатель):** Реализован в `patterns/Observer.js`. Система подписки на события: при добавлении нового экспоната автоматически срабатывает логирование.
*   **Strategy (Стратегия):** Реализован в `patterns/SortStrategy.js`. Позволяет на лету менять алгоритмы сортировки данных без изменения кода контроллера.
*   **Producer-Consumer:** Реализован в `patterns/Queue.js`. Имитация очереди для фоновой обработки изображений.

## 📂 Структура проекта

```text
/my-museum-project
├── /config             # Конфигурация (Singleton)
├── /controllers        # Логика обработки запросов
├── /models             # Схемы данных
├── /patterns           # Реализация паттернов (Factory, Observer, etc.)
├── /public             # Фронтенд (HTML, Картинки)
├── /views              # EJS шаблоны
├── server.js           # Точка входа (Demo режим)
└── app.js              # Основной файл приложения
